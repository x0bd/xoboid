---
import { Image } from 'astro:assets';
import ProjectCard from '../../components/ProjectCard.astro'
import projects from "../../data/projects";
import Layout from "../../layouts/Layout.astro";


const formatTechs = (values: string[]) =>
	values.toString().replaceAll(",", " • ");
---

<Layout title="All Projects">
	<main class="flex flex-col gap-20">
		<h1 class="text-2xl text-white">Things I built with my computer</h1>
		{projects.length === 0 && <p>Cooking Something |>**<|</p>}

        <section class="flex flex-col gap-4">
            {
                projects.length !== 0 &&  projects.map((project)  => !project.isComingSoon && <ProjectCard {...project} />)
            }
        </section>
	</main>
</Layout>
